{
  "name": "substitute-string-action",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Substitute or replace a string of a text or a file",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --outfile=dist/index.js --platform=node --target=node20",
    "format": "prettier --write index.js"
  },
  "keywords": [
    "actions",
    "substitute"
  ],
  "author": "Bjorn Lu",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "prettier": "^3.2.5"
  }
}
